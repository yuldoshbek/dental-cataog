# ── Frontend (Vite build-time) ────────────────────────────────────────────
# Для production на VPS — оставить пустым (API на том же домене через nginx)
VITE_API_URL=

# Номер WhatsApp менеджера (только цифры, без +): 79001234567
VITE_WHATSAPP_PHONE=
